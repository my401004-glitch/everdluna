# 💻 코다리 — business 에이전트가 확정한 P0/P1 우선순위를 기반으로, 코다리가 즉시 착수해야 할 핵심 DB 스키마 리팩토링 계획과 기술적 요구사항을 구체화하라.

💻 코다리: 작업 시작합니다.

CEO 지시와 현빈 에이전트가 확정한 P0/P1 우선순위(기술적 안정성 및 수익화 기반)를 검토했습니다. 현재 저희의 가장 큰 리스크는 **'신뢰할 수 있는 데이터 흐름과 접근 제어'**입니다. 마케팅이나 기능 구현 이전에, 모든 비즈니스 로직이 트랜잭션 경계 내에서 정확하게 실행되고 기록되는 것이 필수적입니다.

따라서 저는 DB 스키마를 중심으로 기술 아키텍처의 근본적인 리팩토링을 진행해야 한다고 판단했습니다. 단순히 테이블을 추가하는 것을 넘어, **어떻게 데이터가 흐르고 누가 접근할 수 있는지**에 대한 로직(RBAC & Transaction) 레벨에서 재설계가 필요합니다.

다음은 P0 요구사항을 충족시키기 위한 DB 스키마 리팩토링 계획 및 기술적 구현 요구사항입니다.

---

### ⚙️ 핵심 목표: 신뢰성 확보 (P0 - 권한 게이팅 & 트랜잭션)

현재 구조는 데이터 수집(Write)과 사용자 진단 결과 조회(Read)가 분리되어 있어, 유료 전환 시점의 데이터 불일치나 무결성 문제가 발생할 가능성이 높습니다. 이를 방지하기 위해 스키마와 API 레이어 모두를 수정해야 합니다.

#### 1. DB 스키마 리팩토링 계획 (Schema Refactoring)

기존 `schema.sql`을 확장하여 다음 핵심 테이블과 관계(Relationship)를 추가하고, 외래 키(Foreign Key) 제약을 강화해야 합니다.

**
